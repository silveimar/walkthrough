---
phase: 01-policy-contract-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - security/security-policy.json
  - security/security-policy.schema.json
  - security/validate-policy.mjs
  - security/verify-policy.mjs
  - scripts/verify-policy
autonomous: true
requirements:
  - POL-02
user_setup: []
must_haves:
  truths:
    - "Repository contains canonical policy at security/security-policy.json (D-02)."
    - "Invalid or incomplete policy documents fail validation with non-zero exit (D-03, D-16)."
    - "verify-policy emits machine-readable JSON and a human-readable summary (D-15)."
  artifacts:
    - path: "security/security-policy.json"
      provides: "Authoritative local-only and egress defaults for the repo"
      contains: "version"
    - path: "security/verify-policy.mjs"
      provides: "CLI verification entry"
      exports: []
    - path: "scripts/verify-policy"
      provides: "Shell wrapper (D-13 primary surface)"
  key_links:
    - from: "scripts/verify-policy"
      to: "security/verify-policy.mjs"
      via: "exec node with repo root"
      pattern: "verify-policy"
---

<objective>
Create the canonical JSON security policy, a strict JSON Schema, and a fail-closed `verify-policy` command that validates the policy document and reports results in both JSON and human-readable form.

Purpose: Establishes single source of truth and verification surface for POL-02 and all downstream gating (D-01–D-04, D-13–D-16).
Output: `security/security-policy.json`, `security/security-policy.schema.json`, `security/validate-policy.mjs` (shared validation logic), `security/verify-policy.mjs`, `scripts/verify-policy`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-policy-contract-foundation/01-CONTEXT.md

Policy keys are authoritative for later phases; document field names in schema `description` fields so docs can reference keys without duplicating rule prose (D-04).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonical policy document and JSON Schema</name>
  <files>security/security-policy.json, security/security-policy.schema.json</files>
  <action>
Implement per D-01, D-02, D-05–D-08, D-10, D-12: JSON policy MUST include at minimum explicit keys for (1) schema version, (2) default egress posture (`deny_all` / equivalent enum), (3) local-only default `true`, (4) LLM/rubric network default `off`, (5) ordered allowlist structure for per-run egress exceptions (empty default), (6) `entrypoints` or equivalent list of gated script paths/basenames that MUST be policy-aware for D-10, (7) publish/pre-publish approved path roots for static workflow (list or glob semantics — choose one and document in schema `description`). Schema MUST use `additionalProperties: false` at each object level where feasible so unknown keys fail validation (D-03). Required properties MUST be listed in schema `required` arrays. Do not duplicate behavioral prose in README here — schema descriptions anchor doc references (D-04).
  </action>
  <verify>
    <automated>test -f security/security-policy.json &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('security/security-policy.json','utf8'))" &amp;&amp; test -f security/security-policy.schema.json &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('security/security-policy.schema.json','utf8'))"</automated>
  </verify>
  <done>Policy and schema are valid JSON; after Task 2, example policy validates via validate-policy module.</done>
</task>

<task type="auto">
  <name>Task 2: Strict validator module (fail-closed)</name>
  <files>security/validate-policy.mjs</files>
  <action>
Implement Node ESM module that loads `security/security-policy.json` and validates against `security/security-policy.schema.json` without adding npm dependencies if possible: use structural validation sufficient for fail-closed behavior (required keys present, unknown keys rejected per schema). Export `validatePolicyDocument(parsed)` returning `{ ok, errors[] }` and throw or return detailed errors for CLI. Any missing required key or unknown key MUST yield ok=false (D-03). Reserve extension point for JSON Schema via dynamic import only if you add a minimal package.json with a validator dependency — prefer zero new deps unless unavoidable; if ajv is added, record in a root package.json with engines node &gt;=18 only.
  </action>
  <verify>
    <automated>node --check security/validate-policy.mjs</automated>
  </verify>
  <done>Unit-level check: corrupt copy of policy (rename in test) fails validation with explicit error messages listing keys/paths.</done>
</task>

<task type="auto">
  <name>Task 3: verify-policy CLI and scripts/verify-policy wrapper</name>
  <files>security/verify-policy.mjs, scripts/verify-policy</files>
  <action>
Implement CLI `node security/verify-policy.mjs` invoked from repo root: reads canonical path `security/security-policy.json` (D-02), runs validator, prints human-readable summary to stderr or stdout (pick one convention and stay consistent), prints machine-readable JSON object as final line or separate fd documenting `{ valid, errors, policyPath }` (D-15). Exit 0 only when valid (D-16). Add executable bash wrapper `scripts/verify-policy` that resolves repo root from script location and invokes node CLI (D-13). Document env vars only if needed for policy path override — default MUST be canonical path.
  </action>
  <verify>
    <automated>chmod +x scripts/verify-policy &amp;&amp; bash scripts/verify-policy; echo exit:$?</automated>
  </verify>
  <done>Successful run shows summary + JSON; intentional breakage of JSON syntax yields non-zero with actionable message.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk → validator | Policy file is maintainer-controlled but must be validated as untrusted structure |
| Validator → process exit | Exit code must not leak secrets; errors reference keys/paths only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | security/security-policy.json | mitigate | Strict schema + additionalProperties false; validate before use |
| T-01-02 | I | validate-policy.mjs | mitigate | Fail closed on parse/validation errors; no silent defaults for missing keys |
| T-01-03 | E | verify-policy CLI output | accept | Human summary shows validation status only; no embedding of full policy if it contained secrets (policy should not hold secrets) |
</threat_model>

<verification>
Run `bash scripts/verify-policy` from repo root after Tasks 1–3; exit 0. Temporarily rename policy file and confirm non-zero exit with clear error.
</verification>

<success_criteria>
Canonical policy and schema exist; verify-policy wrapper runs locally with JSON + human output and strict failure on invalid documents (POL-02).
</success_criteria>

<output>
After completion, create `.planning/phases/01-policy-contract-foundation/01-01-SUMMARY.md`
</output>
