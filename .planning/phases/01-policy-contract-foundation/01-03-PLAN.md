---
phase: 01-policy-contract-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - .github/workflows/static.yml
  - README.md
  - skills/walkthrough/skill.md
autonomous: true
requirements:
  - POL-01
  - POL-02
user_setup: []
must_haves:
  truths:
    - "CI runs the same verify-policy command as local runs before publish (D-14, D-16)."
    - "README and walkthrough skill reference the canonical policy path and key env/flags by name, not duplicate rules (D-04, D-11)."
    - "Pre-publish step enforces approved publish roots from policy (D-12) or fails closed."
  artifacts:
    - path: ".github/workflows/static.yml"
      provides: "Policy gate before pages artifact upload"
    - path: "README.md"
      provides: "Pointer to security/security-policy.json and verify-policy"
    - path: "skills/walkthrough/skill.md"
      provides: "Required policy contract reference for authors"
  key_links:
    - from: "README.md"
      to: "security/security-policy.json"
      via: "path link or prose"
    - from: ".github/workflows/static.yml"
      to: "scripts/verify-policy"
      via: "run step"
      pattern: "verify-policy"
---

<objective>
Add CI verification and documentation/skill references so hardening behavior is single-sourced to the policy file (POL-02) and the same checks run locally and in GitHub Actions (POL-01/POL-02 cross-check via D-14).

Purpose: D-11, D-12, D-14–D-16 — publish pipeline cannot skip policy; docs point to machine-readable source.
Output: updated `.github/workflows/static.yml`, `README.md`, `skills/walkthrough/skill.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-policy-contract-foundation/01-CONTEXT.md
@.github/workflows/static.yml

Current workflow uploads entire repository as artifact. Per D-12, add a hard gate: run `bash scripts/verify-policy` on ubuntu-latest with Node 18+ (match repo stack), then add a second guard that the upload `path` is restricted to policy-approved directories (read approved roots from `security/security-policy.json` with `node -e` or a tiny `security/print-publish-roots.mjs` re-export from policy-runtime). If narrowing upload path is incompatible with current Pages needs, set `path` to the minimal approved subtree that still includes `examples/` and docs per policy `publish` section — document the allowed roots in policy (Plan 01) and fail workflow if `actions/upload-pages-artifact` path is not under those roots (assertion script).
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions policy gate and publish scope</name>
  <files>.github/workflows/static.yml</files>
  <action>
After checkout: setup Node 18+ (`actions/setup-node@v4` with `node-version: 18` or `20`). Run `bash scripts/verify-policy` from repo root; non-zero must fail the job (D-16, D-14). Insert step before `actions/upload-pages-artifact` that validates the artifact `path` (or the intended publish root) is within policy-approved paths from `security/security-policy.json` (D-12) — implement via one-line `node` import of `security/policy-runtime.mjs` to print/validate path, or inline JSON read + path prefix check. If full-repo upload is required temporarily, assert at minimum that verify-policy passed and `path` is explicitly set to a variable checked against policy; do not leave publish unrestricted without a failing check when policy demands roots.
  </action>
  <verify>
    <automated>test -f .github/workflows/static.yml &amp;&amp; grep -q "verify-policy" .github/workflows/static.yml &amp;&amp; grep -q "setup-node" .github/workflows/static.yml</automated>
  </verify>
  <done>Workflow contains setup-node, verify-policy step, and publish-scope check before upload; invalid policy fails CI.</done>
</task>

<task type="auto">
  <name>Task 2: README security section</name>
  <files>README.md</files>
  <action>
Add concise **Security / local-only policy** section: point to `security/security-policy.json` as single source (D-04, POL-02), list `bash scripts/verify-policy`, document env/CLI flags for egress and LLM rubric opt-in by exact names matching policy keys (no second copy of default values that could drift). Mention eval entrypoints are policy-gated (D-09) at high level.
  </action>
  <verify>
    <automated>grep -n "security/security-policy.json" README.md &amp;&amp; grep -n "verify-policy" README.md</automated>
  </verify>
  <done>README references canonical path and verify command; no contradictory duplicate rules.</done>
</task>

<task type="auto">
  <name>Task 3: Walkthrough skill policy reference</name>
  <files>skills/walkthrough/skill.md</files>
  <action>
In frontmatter `metadata` or a short **Security** subsection after frontmatter, reference `security/security-policy.json` and the verify command so runtime consumers see required contract (D-11). Use one paragraph; link paths relative to repo root. Do not restate numeric thresholds from policy — reference keys.
  </action>
  <verify>
    <automated>grep -n "security/security-policy.json" skills/walkthrough/skill.md</automated>
  </verify>
  <done>Skill doc mentions policy file path; frontmatter valid YAML.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo | Workflow only reads policy; secrets not required for verify-policy |
| Docs → maintainer | Docs must not introduce shadow policies conflicting with JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | static.yml | mitigate | Run verify-policy; restrict publish path per policy |
| T-01-08 | I | README/skill copy | mitigate | Reference keys and paths only; no parallel rule sets |
</threat_model>

<verification>
`grep` checks from tasks; local `act` or manual workflow not required. Optional: `bash scripts/verify-policy` locally.
</verification>

<success_criteria>
CI and docs align with POL-02 single source; CI runs same verifier as local (POL-01/02 operational consistency).
</success_criteria>

<output>
After completion, create `.planning/phases/01-policy-contract-foundation/01-03-SUMMARY.md`
</output>
