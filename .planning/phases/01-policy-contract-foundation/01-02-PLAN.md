---
phase: 01-policy-contract-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - security/policy-runtime.mjs
  - evals/run.sh
  - evals/graders/llm-rubric.mjs
  - evals/graders/deterministic.mjs
  - evals/report.mjs
autonomous: true
requirements:
  - POL-01
  - POL-02
user_setup: []
must_haves:
  truths:
    - "evals/run.sh runs verify-policy before eval work and enforces local-only egress defaults (D-09, D-05, D-07)."
    - "LLM rubric grader does not invoke network-capable `claude` unless policy + explicit opt-in align (D-08, D-06)."
    - "deterministic.mjs and report.mjs load policy and abort if policy validation/guard fails (D-09, D-10)."
  artifacts:
    - path: "security/policy-runtime.mjs"
      provides: "Shared loadPolicy, egress guard, LLM opt-in checks"
      exports: ["loadRepoPolicy", "assertEvalGuards", "describeBlockedEgress"]
    - path: "evals/run.sh"
      provides: "Preflight verify-policy + egress flags"
      contains: "verify-policy"
    - path: "evals/graders/llm-rubric.mjs"
      provides: "Policy gate before execSync to claude"
  key_links:
    - from: "evals/run.sh"
      to: "scripts/verify-policy"
      via: "bash preflight"
      pattern: "scripts/verify-policy"
    - from: "evals/graders/llm-rubric.mjs"
      to: "security/policy-runtime.mjs"
      via: "import"
      pattern: "policy-runtime"
---

<objective>
Wire all Phase-1 eval entrypoints to the canonical policy: preflight verification, local-only egress enforcement with explicit allowlist alignment (POL-01), and shared policy reads (POL-02).

Purpose: Satisfies D-05–D-11: deny-all egress by default, explicit exceptions, actionable errors, LLM opt-in, and blocking execution if guards are missing.
Output: `security/policy-runtime.mjs` plus integrated `evals/run.sh`, `evals/graders/*.mjs`, `evals/report.mjs`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-policy-contract-foundation/01-CONTEXT.md
@evals/run.sh
@evals/graders/llm-rubric.mjs

Existing `llm-rubric.mjs` uses `execSync` to call `claude -p` — that is the network-capable path requiring explicit opt-in per D-08. `run.sh` invokes `claude -p` for prompts — treat as egress-capable subprocess; local-only deny must block unless policy allowlist + matching CLI flags/env per D-06.

Interfaces to implement in `security/policy-runtime.mjs`:
- Resolve repo root from `import.meta.url` (ESM) relative to `security/` directory.
- `loadRepoPolicy()` returns parsed validated policy (reuse validate-policy.mjs exports from Plan 01 — import same validation entry or shared module).
- `assertEvalGuards({ scriptId, env, argv })` throws Error with message including blocked target detail (D-07) when egress would violate policy.
- For bash-driven allowlist flags, mirror minimal parsing in run.sh: e.g. `--allow-egress` comma list must match policy allowlist entries exactly or substring per discretion — document in thrown error.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Policy runtime module</name>
  <files>security/policy-runtime.mjs</files>
  <action>
Implement ESM module exporting `loadRepoPolicy()`, `assertEgressAllowedForRun({ channel, requestId, env, argvFlags })` (or equivalent names) that read `security/security-policy.json` (D-02) and apply D-05–D-08. Include helper for LLM rubric: `assertLlmRubricNetworkAllowed(env)` that enforces off-by-default and requires explicit env (e.g. `WALKTHROUGH_LLM_RUBRIC=1` or policy-defined key) plus matching policy flag. All failures throw Error whose message includes what was blocked and which policy key governs it (D-07). Reuse validation from `security/validate-policy.mjs` (Plan 01) so invalid on-disk policy aborts before any eval work.
  </action>
  <verify>
    <automated>node --check security/policy-runtime.mjs</automated>
  </verify>
  <done>Module loads without side effects on import; calling guard functions with deny posture and no allow flags throws (smoke via `node -e` import assertion in verify command).</done>
</task>

<task type="auto">
  <name>Task 2: evals/run.sh preflight and egress flags</name>
  <files>evals/run.sh</files>
  <action>
At start (after `set` and `SCRIPT_DIR` resolution, before eval loop per D-09, D-14): invoke `bash "$PROJECT_DIR/scripts/verify-policy"`; on non-zero exit, print stderr and exit same code (D-16). Add flags per D-06: e.g. `--allow-egress` with value list and/or env vars documented in policy; when local-only default is deny, refuse to start `claude -p` unless allowlist + flags align; on block, echo actionable error including requested channel/target (D-07). Keep existing behavior when policy allows (compatibility). Wire `SKIP_LLM` to remain consistent with policy LLM default off (D-08) — if policy says LLM off, require explicit opt-in to run LLM grader even when HTML exists.
  </action>
  <verify>
    <automated>bash -n evals/run.sh</automated>
  </verify>
  <done>Script syntax valid; preflight call present; dry-run path: `bash -n` and grep confirms `verify-policy` and new flag parsing exist.</done>
</task>

<task type="auto">
  <name>Task 3: Gate Node entrypoints</name>
  <files>evals/graders/llm-rubric.mjs, evals/graders/deterministic.mjs, evals/report.mjs</files>
  <action>
At top-level after imports, call shared guard: load policy and `assert*Guards` appropriate to each script. `llm-rubric.mjs` MUST call LLM network guard before `execSync` to claude (D-08, D-09). `deterministic.mjs` and `report.mjs` MUST validate policy load succeeds and that script is an approved entrypoint in policy metadata (if policy lists `entrypoints.allowed` — if not in Plan 01 schema, add optional list in Plan 01 follow-up OR hardcode script basename check documented in policy-runtime only — prefer adding `entrypoints` to policy in execution if Plan 01 schema already allows extension). If entrypoint list exists in policy from Plan 01, enforce D-10 by exiting non-zero when script name not listed. Use consistent process.exit(1) with stderr message on violation.
  </action>
  <verify>
    <automated>node --check evals/graders/llm-rubric.mjs &amp;&amp; node --check evals/graders/deterministic.mjs &amp;&amp; node --check evals/report.mjs</automated>
  </verify>
  <done>Each file imports policy-runtime or validate path; llm-rubric cannot reach execSync when policy forbids LLM network and env opt-in missing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env/argv → policy-runtime | Untrusted invoker can set flags; must match policy allowlist |
| policy-runtime → subprocess | Blocks accidental egress; errors must not leak sensitive paths from unrelated repos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate |
|-----------|----------|-----------|-------------|----------|
| T-01-04 | S | run.sh claude invocation | mitigate | Default deny; allow only with explicit policy + flag |
| T-01-05 | E | llm-rubric execSync | mitigate | Policy + opt-in before network call |
| T-01-06 | I | report.mjs / deterministic.mjs | mitigate | Fail if policy missing or entrypoint not approved |
</threat_model>

<verification>
From repo root: `bash scripts/verify-policy` then `node -e "import('./security/policy-runtime.mjs').then(m =&gt; m.loadRepoPolicy())"` (adjust exports); run `bash -n evals/run.sh`.
</verification>

<success_criteria>
POL-01 satisfied: default posture blocks unintended egress unless explicit allow matches policy. POL-02 satisfied: scripts consume canonical policy file via shared runtime (POL-02).
</success_criteria>

<output>
After completion, create `.planning/phases/01-policy-contract-foundation/01-02-SUMMARY.md`
</output>
