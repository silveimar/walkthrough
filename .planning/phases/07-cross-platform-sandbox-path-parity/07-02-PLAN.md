---
phase: 07-cross-platform-sandbox-path-parity
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - evals/run.sh
  - security/policy-runtime.mjs
  - scripts/
autonomous: true
requirements:
  - PLT-02
must_haves:
  truths:
    - "Path/temp behavior for eval workspace checks is consistent with documented matrix; bash uses portable patterns for PROJECT_DIR and work dirs."
    - "Focused automated tests cover tmpdir / path prefix rules without requiring Windows CI."
  artifacts:
    - path: security/policy-runtime.mjs
      provides: "Path assertions remain canonical; any new helper is documented"
      contains: "assertEvalWorkspaceDirAllowed"
    - path: evals/run.sh
      provides: "Portable directory resolution for harness"
      contains: "PROJECT_DIR"
  key_links:
    - from: evals/run.sh
      to: security/policy-runtime.mjs
      via: "Node policy checks / vendor verify"
      pattern: "policy-runtime"
---

<objective>
Deliver **PLT-02**: **normalized path and temp-workspace behavior** across the supported matrix per **07-CONTEXT D-03, D-04** — portable **bash** in `evals/run.sh` for orchestration; **policy-runtime.mjs** owns invariant assertions (`realpath`, temp root). Add **focused Node tests** (new script under `scripts/` or `evals/` test pattern) that exercise `assertEvalWorkspaceDirAllowed` / path helpers on **ubuntu CI** so regressions surface without a Windows runner.

Purpose: Reduces “works on my machine” drift for workspace location and path joining.

Output: Incremental `evals/run.sh` hardening only where needed; optional small exports from `policy-runtime.mjs` for testability; `node` test file runnable in CI; `bash scripts/verify-policy` still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cross-platform-sandbox-path-parity/07-CONTEXT.md
@.planning/phases/07-cross-platform-sandbox-path-parity/07-RESEARCH.md
@security/policy-runtime.mjs
@evals/run.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit bash path usage in evals/run.sh</name>
  <files>evals/run.sh</files>
  <read_first>evals/run.sh</read_first>
  <action>
Review temp workspace creation and `PROJECT_DIR` usage. Apply **POSIX-safe** patterns; avoid assumptions that break Git Bash (e.g. mixed slashes). Document notable choices in comments only where non-obvious.

Do **not** rewrite unrelated eval flow (prompts, Claude invocation).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -n evals/run.sh</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Node tests for workspace / path rules</name>
  <files>scripts/test-path-workspace.mjs, security/policy-runtime.mjs</files>
  <read_first>security/policy-runtime.mjs</read_first>
  <action>
Add a **small** Node script (name flexible) that:
- Creates a temp dir under `os.tmpdir()`, calls `assertEvalWorkspaceDirAllowed` when policy requires temp (respect existing policy flags), and asserts **failure** for an obviously invalid directory when that guard is enabled **or** tests helper behavior via exported pure functions if easier.
- Exit 0 on success; suitable for `node scripts/test-path-workspace.mjs` from repo root.

Keep scope minimal—integration smoke, not full cross-platform matrix simulation.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node scripts/test-path-workspace.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Script exists and exits 0 locally
    - Does not require Claude CLI or network
  </acceptance_criteria>
</task>

</tasks>
