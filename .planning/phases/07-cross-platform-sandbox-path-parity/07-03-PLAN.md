---
phase: 07-cross-platform-sandbox-path-parity
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - PLT-03
  - PLT-04
must_haves:
  truths:
    - "CI sets WALKTHROUGH_STRICT_CWD=1 for at least one relevant Node verification step (CONTEXT D-06)."
    - "Core flows remain runnable without Docker; no new container requirement."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "STRICT_CWD env + optional new test step"
      contains: "WALKTHROUGH_STRICT_CWD"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/test-path-workspace.mjs
      via: "invoke path smoke if present"
      pattern: "test-path-workspace"
---

<objective>
Deliver **PLT-03** and **automation slice of PLT-04**: enable **`WALKTHROUGH_STRICT_CWD=1`** in CI per **07-CONTEXT D-06**; wire **`node scripts/test-path-workspace.mjs`** (from Plan 07-02) into the workflow when that file exists; keep **Docker optional** (**D-07**).

Purpose: Automated signal on cwd misuse and path workspace rules on every PR—complements docs/manual smoke from 07-01.

Output: Updated `.github/workflows/ci.yml`; green local + CI semantics preserved for existing steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cross-platform-sandbox-path-parity/07-CONTEXT.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strict cwd + path smoke in CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml, security/policy-runtime.mjs</read_first>
  <action>
1. Export **`WALKTHROUGH_STRICT_CWD=1`** for the job or for specific steps that run `node --check` / Node scripts that import `policy-runtime.mjs`—ensure **`assertStrictRepoWorkingDirectory`** actually runs in CI context (working directory is repo root on `actions/checkout`).

2. Add a step **after** `node --check` loop (or integrated): `node scripts/test-path-workspace.mjs` **if** file exists—if 07-02 not merged yet, guard with file existence or land plans in order.

3. Confirm no Docker prerequisite is introduced.

Order of merging: if executing phases out of order, make CI resilient (conditional step).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" 2>/dev/null || yq '.jobs' .github/workflows/ci.yml >/dev/null 2>&1 || true</automated>
  </verify>
  <acceptance_criteria>
    - YAML remains valid
    - STRICT_CWD appears in CI config
  </acceptance_criteria>
</task>

</tasks>
