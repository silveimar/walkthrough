---
phase: 07-cross-platform-sandbox-path-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - PLT-01
  - PLT-04
must_haves:
  truths:
    - "Maintainer-facing docs define a supported platform matrix (shell, Node, OS) and what parity means for this repo."
    - "Manual smoke checklist exists for Git Bash and WSL2 covering verify-policy and shell syntax paths."
  artifacts:
    - path: README.md
      provides: "Links or sections for platform matrix / parity"
      contains: "parity"
    - path: CONTRIBUTING.md
      provides: "Platform matrix + manual smoke steps"
      contains: "Git Bash"
  key_links:
    - from: CONTRIBUTING.md
      to: README.md
      via: "cross-links"
      pattern: "walkthrough"
---

<objective>
Deliver **PLT-01** and the **documentation slice of PLT-04**: a clear **supported platform matrix**, definition of **parity** (consistent policy + harness semantics, not identical OS sandboxes), and **manual smoke steps** for **Git Bash** and **WSL2**—per **07-CONTEXT D-01, D-02, D-05**.

Purpose: Establishes the contract maintainers follow before optional Windows CI lands.

Output: Updated `README.md` and/or new `CONTRIBUTING.md` (or `docs/` note—executor chooses minimal path per CONTEXT) with matrix + checklist; cross-links so discoverability is obvious.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cross-platform-sandbox-path-parity/07-CONTEXT.md
@.planning/phases/07-cross-platform-sandbox-path-parity/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Platform matrix + parity definition</name>
  <files>README.md, CONTRIBUTING.md</files>
  <read_first>.planning/phases/07-cross-platform-sandbox-path-parity/07-CONTEXT.md, README.md</read_first>
  <action>
Add a **Supported platforms** (or equivalent) section covering **macOS**, **Linux**, **Git Bash on Windows**, **WSL2**. State **Node** minimum (align with README: >= 18). Define **parity** exactly as in CONTEXT: same Node + bash entrypoints produce consistent policy interpretation—not identical OS sandboxes.

If `CONTRIBUTING.md` does not exist, create it from a short maintainer-focused template and link from README.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && rg -n "Git Bash|WSL|parity" README.md CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - Matrix lists shell + OS expectations for each row
    - Parity definition is explicit and matches CONTEXT
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Manual smoke checklist (PLT-04)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>evals/run.sh, .github/workflows/ci.yml</read_first>
  <action>
Document **manual smoke** steps a maintainer runs on Windows (two subsections: **Git Bash**, **WSL2**). Minimum commands: `bash scripts/verify-policy`, `bash -n evals/run.sh`, and `node --check` on `security/policy-runtime.mjs` (match CI spirit). Note Claude CLI is eval-only; keep optional.

Align wording with **D-05**: docs-first; no mandate for GitHub Windows runner in this plan.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && rg -n "verify-policy|bash -n" CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - Both Git Bash and WSL2 have explicit steps
    - Commands are copy-paste friendly
  </acceptance_criteria>
</task>

</tasks>
