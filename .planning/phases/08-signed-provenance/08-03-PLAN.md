---
phase: 08-signed-provenance
plan: 03
type: execute
wave: 3
depends_on:
  - "08-02-PLAN.md"
files_modified:
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
autonomous: true
requirements:
  - ATT-05
must_haves:
  truths:
    - "Optional CI verify job or documented toggle exists; default workflow does not require maintainers to possess signing keys."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "optional provenance verify"
      contains: "verify-provenance"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/verify-provenance.mjs
      via: "conditional step"
      pattern: "verify-provenance"
---

<objective>
Deliver **ATT-05**: **optional** CI path—either a **disabled-by-default** job, **`workflow_dispatch`**, or env-gated step calling **`node scripts/verify-provenance.mjs`** when signature + pubkey exist. Document in **CONTRIBUTING** how teams enable required checks.

Purpose: Teams can turn on merge gates without blocking contributors who do not sign.

</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-signed-provenance/08-CONTEXT.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optional CI verify</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Add job or step that runs only when condition is met (e.g. `secrets.VERIFY_PROVENANCE == 'true'` may not work—prefer `github.event_name == workflow_dispatch` or repository variable). Simplest acceptable: **commented** optional job block + **CONTRIBUTING** section "Enabling provenance verify in CI".

If clean workflow syntax allows: `if: vars.ENABLE_PROVENANCE_VERIFY == 'true'` (requires repo variable—document).

Fallback: **document-only** ATT-05 with explicit "copy-paste" CI snippet—acceptable if workflow file gains a clear placeholder.

Choose minimal blast radius: default CI unchanged.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && rg -n "verify-provenance|provenance" .github/workflows/ci.yml CONTRIBUTING.md</automated>
  </verify>
</task>

</tasks>
