---
phase: 08-signed-provenance
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01-PLAN.md"
files_modified:
  - scripts/build-provenance-manifest.mjs
  - scripts/verify-provenance.mjs
  - .gitignore
autonomous: true
requirements:
  - ATT-02
  - ATT-03
  - ATT-04
must_haves:
  truths:
    - "Build script emits canonical provenance manifest JSON from repo files."
    - "Verify script recomputes digests and runs minisign verify when .sig present."
  artifacts:
    - path: scripts/build-provenance-manifest.mjs
      provides: "writes provenance/manifest.json"
      contains: "policySha256"
    - path: scripts/verify-provenance.mjs
      provides: "verification entrypoint"
      contains: "minisign"
  key_links:
    - from: scripts/verify-provenance.mjs
      to: provenance/README.md
      via: "documented workflow"
      pattern: "provenance"
---

<objective>
Deliver **ATT-02**, **ATT-03**, **ATT-04**: **`scripts/build-provenance-manifest.mjs`** computes SHA-256 of policy JSON and vendor manifest JSON, writes **`provenance/manifest.json`**; maintainer signs with **Minisign** (documented command); **`scripts/verify-provenance.mjs`** validates digests + signature. **`.gitignore`** Minisign secret keys; optional **`provenance/minisign.pub`** placeholder or docs-only pubkey path.

Purpose: Executable local sign/verify loop without CI dependency.

</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-signed-provenance/08-CONTEXT.md
@security/policy-runtime.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: build-provenance-manifest.mjs</name>
  <files>scripts/build-provenance-manifest.mjs</files>
  <action>
Read files from repo root; output JSON matching schema from 08-01. Exit non-zero if vendor manifest or policy missing.

Do not embed evals/results paths.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node scripts/build-provenance-manifest.mjs && test -f provenance/manifest.json</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: verify-provenance.mjs</name>
  <files>scripts/verify-provenance.mjs</files>
  <action>
Recompute hashes; compare to manifest; if `provenance/manifest.json.minisig` (or chosen path) exists, shell out to `minisign verify` or document subprocess. Graceful skip if signature absent with clear message (unsigned manifest OK for dev).

Enforce repo-root cwd if policy scripts expect it.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node scripts/verify-provenance.mjs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: gitignore secrets</name>
  <files>.gitignore</files>
  <action>
Ignore `*.minisig.key`, `*.key`, or Minisign secret paths documented in provenance/README.md.

Do not ignore public keys if stored as `*.pub`.
  </action>
  <verify>
    <automated>rg -n "minisign|\\.key" .gitignore provenance/README.md</automated>
  </verify>
</task>

</tasks>
