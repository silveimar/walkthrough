---
phase: 08-signed-provenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - provenance/
  - README.md
autonomous: true
requirements:
  - ATT-01
  - ATT-04
must_haves:
  truths:
    - "Documented manifest-first model lists in-scope conceptual artifacts (policy digest, vendor manifest digest) and explicit exclusions (evals/results, sensitive outputs)."
  artifacts:
    - path: provenance/README.md
      provides: "Manifest-first model for maintainers"
      contains: "manifestVersion"
  key_links:
    - from: provenance/README.md
      to: security/security-policy.json
      via: "policySha256"
      pattern: "security/"
---

<objective>
Deliver **ATT-01** and documentation slice of **ATT-04**: **manifest-first** documentation—what bytes are represented (via digests of `security/security-policy.json` and `vendor/walkthrough-viewer/manifest.json`), what is **out of scope** (eval results, redacted material), and how this relates to `dataProtection` in policy.

Output: `provenance/README.md` (or equivalent) + optional **JSON Schema** stub for provenance manifest shape; link from root **README** if appropriate.

Purpose: Lock semantics before scripts in wave 2.

</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-signed-provenance/08-CONTEXT.md
@security/security-policy.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: provenance/README.md manifest-first spec</name>
  <files>provenance/README.md</files>
  <action>
Describe: signed payload is **compact manifest JSON** with `manifestVersion`, `policySha256`, `vendorManifestSha256`, optional metadata (timestamp note: generated not signed clock oracle). List **exclusions**: never attest paths under canonical eval results or secret-like outputs per policy.

Reference Minisign as primary signing tool (per CONTEXT D-01).
  </action>
  <verify>
    <automated>test -f provenance/README.md && rg -q "policySha256|vendorManifestSha256|evals/results" provenance/README.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Schema or template</name>
  <files>provenance/manifest.schema.json</files>
  <action>
Add minimal JSON Schema for the provenance manifest fields planned in CONTEXT (integer manifestVersion, hex sha fields).

Do not commit generated `manifest.json` until wave 2 unless needed as example—prefer documenting shape only.
  </action>
  <verify>
    <automated>test -f provenance/manifest.schema.json</automated>
  </verify>
</task>

</tasks>
