---
phase: 06-offline-vendor-and-dual-mode-html
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - skills/walkthrough/references/html-patterns.md
  - examples/walkthrough-how-it-works.html
  - evals/graders/deterministic.mjs
autonomous: true
requirements:
  - OFF-01
  - OFF-05
must_haves:
  truths:
    - "html-patterns.md documents both CDN and vendor-relative canonical snippets with explicit dual-mode rules (per D-01/D-02 discretion documented, not silent)."
    - "examples/walkthrough-how-it-works.html reflects the documented pattern(s) as the repo reference artifact."
    - "deterministic.mjs Tier 2 asserts CDN markers OR vendor-relative markers consistent with resolved policy via policy-runtime — not CDN-only substring gates."
  artifacts:
    - path: skills/walkthrough/references/html-patterns.md
      provides: "Dual-mode HTML contract for skill-generated walkthroughs"
      contains: "vendor"
    - path: examples/walkthrough-how-it-works.html
      provides: "Reference wiring for React/Mermaid/Tailwind/Shiki consistent with patterns"
    - path: evals/graders/deterministic.mjs
      provides: "Policy-aware structural checks"
      contains: "resolveAssetSource"
  key_links:
    - from: evals/graders/deterministic.mjs
      to: security/policy-runtime.mjs
      via: "import policy resolution; branch Tier 2 checks by effective assetSource"
    - from: skills/walkthrough/references/html-patterns.md
      to: examples/walkthrough-how-it-works.html
      via: "Example must not contradict documented script/link order and URLs"
---

<objective>
Update the **skill HTML reference** and **example artifact** for explicit **CDN vs vendor** dual mode, and refactor **`evals/graders/deterministic.mjs`** Tier 2 to use **policy-aware** expectations (OFF-05) instead of hard-coded CDN substring checks — enabling **offline-capable** eval interpretation when policy selects vendor (OFF-01).

Purpose: Align author guidance, reference HTML, and graders on one contract produced in 06-01.

Output: Revised `html-patterns.md`, updated `examples/walkthrough-how-it-works.html`, and refactored deterministic grader loading the same policy resolution as runtime.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-offline-vendor-and-dual-mode-html/06-CONTEXT.md
@.planning/phases/06-offline-vendor-and-dual-mode-html/06-RESEARCH.md

@skills/walkthrough/references/html-patterns.md
@examples/walkthrough-how-it-works.html
@evals/graders/deterministic.mjs
@security/policy-runtime.mjs
@security/security-policy.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document dual-mode patterns and align reference HTML</name>
  <files>skills/walkthrough/references/html-patterns.md, examples/walkthrough-how-it-works.html</files>
  <read_first>skills/walkthrough/references/html-patterns.md, examples/walkthrough-how-it-works.html, .planning/phases/06-offline-vendor-and-dual-mode-html/06-RESEARCH.md</read_first>
  <action>
**html-patterns.md:** Add a clear **Dual mode** section documenting:
- **CDN mode**: preserve current canonical URLs / script order as today (React UMD, Mermaid, Tailwind Play CDN + config block, Shiki ESM from CDN) — cite exact patterns already in file.
- **Vendor mode**: document relative paths under `./vendor/walkthrough-viewer/` (or the exact prefix chosen in 06-01 policy `vendorRootRel`) for each asset class: UMD scripts for React/ReactDOM/Mermaid; **precompiled CSS** via `&lt;link href=...&gt;` for Tailwind offline per **D-01** (Option A from RESEARCH — link tag, not `cdn.tailwindcss.com`); Shiki ESM import from vendored package path per **D-02**.
State explicitly that generated HTML must use **one** consistent mode matching resolved policy — no mixed accidental CDN + vendor.

**examples/walkthrough-how-it-works.html:** Keep **one** primary demonstration path consistent with **current** `security/security-policy.json` defaults after 06-01 (likely CDN). If policy defaults remain CDN, update example only as needed for clarity and cross-links to the new dual-mode section; optionally add HTML comments pointing maintainers to vendor snippet location in `html-patterns.md`. Do **not** leave example contradicting Tier 2 expectations after Task 2.

Match existing repo tone and structure in `html-patterns.md` (headings, code fences).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && rg -n "vendor/walkthrough-viewer|Dual mode|CDN mode|Vendor mode" skills/walkthrough/references/html-patterns.md && test -f examples/walkthrough-how-it-works.html</automated>
  </verify>
  <acceptance_criteria>
    - `html-patterns.md` contains explicit CDN and vendor canonical snippets (or clearly labeled subsections)
    - Reference example file exists and includes markers graders still expect after Task 2 (React, Mermaid, Shiki createHighlighter, etc. per mode)
  </acceptance_criteria>
  <done>OFF-05 documentation alignment; example is authoritative for skill consumers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor deterministic.mjs Tier 2 for policy-aware CDN vs vendor checks</name>
  <files>evals/graders/deterministic.mjs</files>
  <read_first>evals/graders/deterministic.mjs, security/policy-runtime.mjs, security/security-policy.json</read_first>
  <behavior>
    - For each logical asset (react, reactDom, tailwind, mermaid, shiki), when policy resolves to **cdn**, existing CDN substring checks (or tightened equivalents) still pass for CDN-generated HTML.
    - When policy resolves to **vendor** for an asset, Tier 2 requires vendor-relative markers (e.g. path contains `./vendor/walkthrough-viewer/` or agreed prefix) instead of CDN hosts for that asset — **not** Tier 2 CDN checks for that asset.
    - Loader failure: if policy file missing or resolution throws, grader should fail closed with a clear message (align with security posture), not silently assume CDN.
  </behavior>
  <action>
Replace Tier 2 **CDN-only** `check(...)` block with branching logic driven by **`resolveAssetSource`** (or exports from `security/policy-runtime.mjs` completed in 06-01). Import policy from the **same path** the grader uses today if it already imports `policy-runtime.mjs`; extend minimally.

Use **package ids** matching `security-policy.json` / schema from 06-01 exactly.

Preserve Tier 1 and other tiers unrelated to CDN/vendor unless they conflict.

Run grader against `examples/walkthrough-how-it-works.html` as a sanity input **after** ensuring example matches default policy mode.

If repo lacks a tiny fixture for vendor-mode HTML, add **`evals/fixtures/`** minimal `walkthrough-vendor-fixture.html` (or similar path) **only** as a second argv test — keep task focused; fixture must satisfy vendor-branch checks when policy is temporarily pointed or duplicated in test — prefer reading policy from disk so maintainer can run `node evals/graders/deterministic.mjs examples/walkthrough-how-it-works.html` only if that remains the CLI contract.

Follow existing `deterministic.mjs` CLI entry patterns (read file path from argv as today).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node evals/graders/deterministic.mjs examples/walkthrough-how-it-works.html</automated>
  </verify>
  <acceptance_criteria>
    - Tier 2 contains no unconditional `unpkg.com` requirement when `resolveAssetSource('react')==='vendor'` (same for other packages)
    - `node evals/graders/deterministic.mjs examples/walkthrough-how-it-works.html` exits 0 under default policy
  </acceptance_criteria>
  <done>OFF-01/OFF-05: graders align with relative vendor vs CDN expectations per policy; offline-capable mode is structurally detectable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated HTML → grader | Attack surface is malformed or deceptive HTML from eval outputs; grader must not accept CDN strings when policy demands vendor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate |
|-----------|----------|-----------|-------------|----------|
| T-06-04 | E | deterministic Tier 2 | mitigate | Branch on `resolveAssetSource`; deny CDN markers when vendor required |
| T-06-05 | I | Policy load in grader | mitigate | Fail if policy invalid; no silent CDN fallback (**D-04** alignment at detection layer) |
| T-06-06 | R | html-patterns.md | mitigate | Explicit dual snippets — reduces ambiguous maintainer HTML |
</threat_model>

<verification>
- [ ] `node evals/graders/deterministic.mjs examples/walkthrough-how-it-works.html` succeeds
- [ ] `rg "resolveAssetSource|policy-runtime" evals/graders/deterministic.mjs` shows wiring
- [ ] Dual-mode section present in html-patterns.md
</verification>

<success_criteria>
- Graders no longer encode CDN-only Tier 2 for assets that can be vendor-sourced
- Skill reference and example consistent with policy contract
</success_criteria>

<output>
After completion, create `.planning/phases/06-offline-vendor-and-dual-mode-html/06-02-SUMMARY.md`
</output>
