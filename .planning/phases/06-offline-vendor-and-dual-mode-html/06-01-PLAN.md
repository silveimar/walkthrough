---
phase: 06-offline-vendor-and-dual-mode-html
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - security/security-policy.schema.json
  - security/security-policy.json
  - security/policy-runtime.mjs
autonomous: true
requirements:
  - OFF-02
  - OFF-04
must_haves:
  truths:
    - "Policy JSON validates via bash scripts/verify-policy with new walkthrough viewer asset keys present."
    - "policy-runtime.mjs exposes deterministic resolution of CDN vs vendor per asset class with documented precedence (per D-03)."
    - "Manifest verification helpers exist for SHA-256 entries against vendor/walkthrough-viewer paths (consumable by later plans)."
  artifacts:
    - path: security/security-policy.schema.json
      provides: "Schema allows granular walkthroughViewerAssets object with per-package overrides"
      contains: "walkthroughViewerAssets"
    - path: security/security-policy.json
      provides: "Concrete defaults and package overrides matching schema"
    - path: security/policy-runtime.mjs
      provides: "resolveAssetSource, loadWalkthroughAssetPolicy, verifyVendorManifest (or equivalent names)"
      contains: "walkthroughViewerAssets"
  key_links:
    - from: security/security-policy.schema.json
      to: security/security-policy.json
      via: "validatePolicyDocument / verify-policy.mjs"
      pattern: "walkthroughViewerAssets"
    - from: security/policy-runtime.mjs
      to: security/security-policy.json
      via: "read and resolve effective assetSource per package id"
---

<objective>
Establish the **policy schema**, **documented policy instance**, and **`policy-runtime.mjs` resolution + manifest verification helpers** for **granular per-asset keys** (D-03). This plan delivers the shared contract for frozen vendor layout declarations (OFF-02) and a single JSON source of truth for runtime + future graders/harness (OFF-04).

Purpose: Downstream waves depend on stable schema and helpers before HTML docs, graders, or `evals/run.sh` behavior change.

Output: Updated `security/security-policy.schema.json`, `security/security-policy.json`, and extended exports in `security/policy-runtime.mjs`; `bash scripts/verify-policy` passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-offline-vendor-and-dual-mode-html/06-CONTEXT.md
@.planning/phases/06-offline-vendor-and-dual-mode-html/06-RESEARCH.md

@security/security-policy.schema.json
@security/security-policy.json
@security/policy-runtime.mjs
@security/validate-policy.mjs
@security/verify-policy.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend schema and policy JSON for granular walkthrough viewer assets</name>
  <files>security/security-policy.schema.json, security/security-policy.json</files>
  <read_first>security/security-policy.schema.json, security/security-policy.json, .planning/phases/06-offline-vendor-and-dual-mode-html/06-RESEARCH.md</read_first>
  <action>
Add a **new top-level property** to the schema (root has `additionalProperties: false` — property must be explicitly declared). Name it per RESEARCH convention, e.g. `walkthroughViewerAssets`, containing at minimum:
- `defaults` with `assetSource` enum `cdn` | `vendor` (single enum style — do not mix conflicting boolean flags).
- `packages` (or equivalent map) with **separate keys per asset class** aligned to graders/HTML: e.g. `react`, `reactDom`, `mermaid`, `tailwind`, `shiki` — each object allowing `assetSource` override and optional `vendorRelPath` (or nested paths under an agreed `vendorRootRel`).
Document **precedence** in schema `description` fields: **package override → defaults** (per D-03).

Update `security/security-policy.json` to populate the new object with values consistent with **current** maintainer posture (e.g. defaults `cdn` until vendor tree lands in Plan 06-03) while exercising **granular keys** — no single global `walkthroughAssetMode` only.

Bump `version` in `security-policy.json` if schema semantics require it (match existing project convention for version bumps).

Do **not** change unrelated policy sections.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash scripts/verify-policy</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-policy` exits 0
    - `security/security-policy.schema.json` defines `walkthroughViewerAssets` (or chosen final name) with per-package granularity
    - `security/security-policy.json` contains populated granular keys, not only a single global mode
  </acceptance_criteria>
  <done>Schema and policy validate together; OFF-04 policy/schema express asset mode and vendor-oriented paths at declarative level.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement policy-runtime resolution and vendor manifest verification helpers</name>
  <files>security/policy-runtime.mjs</files>
  <read_first>security/policy-runtime.mjs, security/security-policy.json, .planning/phases/06-offline-vendor-and-dual-mode-html/06-CONTEXT.md</read_first>
  <action>
Extend `security/policy-runtime.mjs` per **D-03** and RESEARCH:

1. **Load policy slice**: function(s) to read `walkthroughViewerAssets` (final property name must match Task 1) from the validated policy document already used elsewhere in this module — reuse existing policy load path; do not duplicate file IO inconsistently.

2. **`resolveAssetSource(packageId)`** (or equivalent): resolve effective `cdn` vs `vendor` for a named package id (`react`, `shiki`, …) using **documented precedence** — package override → defaults. Export for consumers (`deterministic.mjs`, shell-invoked checks later).

3. **`verifyVendorManifest(repoRoot)`** (or equivalent): read `vendor/walkthrough-viewer/manifest.json` (path fixed by convention from RESEARCH), validate structure (`manifestVersion`, `algorithm: sha256`, `entries` with `path` + `sha256`), and **hash files on disk** under `vendor/walkthrough-viewer/` using Node `crypto.createHash('sha256')` with streaming reads. Return a structured result object `{ ok: boolean, errors: string[] }` (or throw with messages) suitable for **fail-closed** callers — messages aligned with existing egress/redaction tone in repo (**D-04** messaging alignment — implement message style here even though strict exit is Plan 06-03).

4. Export any small helper needed for “does policy require **any** vendor package?” to drive conditional copy in `evals/run.sh` later.

Keep public API names stable and grep-friendly for Wave 2–3 wiring.

If no suitable automated unit test harness exists yet, **do not** block on Jest; verification is `bash scripts/verify-policy` plus a one-shot Node smoke (optional inline `node --input-type=module -e "import ..."` in verify step) that imports `verifyVendorManifest` against repo root — **only if** `vendor/walkthrough-viewer/manifest.json` exists after Plan 06-03; for Wave 1, manifest file may be absent: in that case, implement the function but verify with **mock temp dir** via same `node -e` creating minimal manifest + temp files, OR document **MISSING — Wave 3 adds fixtures** and gate the smoke test on file existence. Prefer implementing logic completely and running **unit-style** inline check with `mktemp` manifest under `/tmp` so Wave 1 is self-verifying without requiring full vendor tree.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash scripts/verify-policy && node --input-type=module -e "import { resolveAssetSource, verifyVendorManifest } from './security/policy-runtime.mjs'; import { fileURLToPath } from 'node:url'; import { mkdirSync, writeFileSync, mkdtempSync } from 'node:fs'; import { join } from 'node:path'; import os from 'node:os'; const root = fileURLToPath(new URL('.', import.meta.url)); const tmp = mkdtempSync(join(os.tmpdir(), 'wt-manifest-')); const vendorRoot = join(tmp, 'vendor/walkthrough-viewer'); mkdirSync(vendorRoot, { recursive: true }); writeFileSync(join(vendorRoot, 'probe.txt'), 'x'); writeFileSync(join(vendorRoot, 'manifest.json'), JSON.stringify({ manifestVersion: 1, algorithm: 'sha256', baseDir: '.', entries: [{ id: 'probe', path: 'probe.txt', sha256: '2d711642b726b04481627d9fc549819cc5d61413176bf494bbb2c040670a36cbb' }] })); const r = verifyVendorManifest(tmp); if (!r.ok) { console.error(r); process.exit(1); } console.log('policy-runtime smoke ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `policy-runtime.mjs` exports resolve + verify functions used by description
    - Precedence for granular keys is encoded once and documented in code comments referencing D-03
    - Inline verify constructs temp vendor layout and proves SHA-256 verification path executes without error
  </acceptance_criteria>
  <done>OFF-02 integrity manifest contract is enforceable in code; OFF-04 runtime shares policy truth with forthcoming graders and run.sh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Policy file → runtime | Untrusted edits to `security-policy.json` must be caught by schema validation before trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | `security-policy.json` | mitigate | Extend schema + `validatePolicyDocument`; reject unknown shape |
| T-06-02 | I | `manifest.json` | mitigate | `verifyVendorManifest` recomputes SHA-256 vs declared digests before trust |
| T-06-03 | E | Policy resolution | mitigate | Single precedence implementation in `policy-runtime.mjs` (no duplicated rules) |
</threat_model>

<verification>
- [ ] `bash scripts/verify-policy` passes
- [ ] Node smoke for `verifyVendorManifest` passes (temp fixture)
- [ ] Grep confirms `walkthroughViewerAssets` (or final chosen name) in schema, policy, and policy-runtime
</verification>

<success_criteria>
- Granular per-asset policy keys implemented per **D-03** with explicit precedence
- Runtime helpers ready for deterministic grader and `evals/run.sh` consumption in Waves 2–3
- No regression in policy validation pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/06-offline-vendor-and-dual-mode-html/06-01-SUMMARY.md`
</output>
