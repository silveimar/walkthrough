---
phase: 06-offline-vendor-and-dual-mode-html
plan: 03
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - evals/run.sh
  - scripts/sync-walkthrough-vendor.mjs
  - scripts/test-vendor-failclosed.mjs
  - vendor/walkthrough-viewer/manifest.json
  - vendor/walkthrough-viewer/README.md
autonomous: true
requirements:
  - OFF-03
  - OFF-06
must_haves:
  truths:
    - "`npm ci` (or documented equivalent) reproduces vendor layout from lockfile at repo root."
    - "`evals/run.sh` copies `vendor/walkthrough-viewer` into the temp workspace when policy requires vendor assets."
    - "When vendor mode is required but tree or manifest is missing/invalid, the harness exits non-zero — **no silent CDN fallback** (D-04)."
  artifacts:
    - path: package-lock.json
      provides: "Pinned transitive versions for OFF-03"
    - path: vendor/walkthrough-viewer/manifest.json
      provides: "SHA-256 integrity manifest for frozen layout"
    - path: vendor/walkthrough-viewer/README.md
      provides: " Maintainer regeneration instructions"
    - path: evals/run.sh
      provides: "Vendor copy + manifest verification + fail-closed preflight"
      contains: "vendor/walkthrough-viewer"
  key_links:
    - from: evals/run.sh
      to: security/policy-runtime.mjs
      via: "Node preflight imports verifyVendorManifest / policy resolution before claude -p"
    - from: evals/run.sh
      to: vendor/walkthrough-viewer/manifest.json
      via: "verify hashes before copy; exit 1 on mismatch"
---

<objective>
Add **pinned npm reproducibility** (`package-lock.json`), a **maintainer vendor sync workflow** (script + docs), bootstrap **`vendor/walkthrough-viewer/`** with integrity manifest, and extend **`evals/run.sh`** to **copy vendor into `WORK_DIR`**, run **manifest verification**, and **fail closed** when vendor mode is required but assets or hashes are wrong (**D-04**, OFF-06). Satisfies **OFF-03** via lockfile-driven installs.

Purpose: Close the loop from policy (06-01) and docs/grader (06-02) to **bytes on disk** and **harness enforcement**.

Output: `package.json`, `package-lock.json`, populated `vendor/walkthrough-viewer/` (tracked files per RESEARCH layout), sync script under `scripts/`, and updated `evals/run.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-offline-vendor-and-dual-mode-html/06-CONTEXT.md
@.planning/phases/06-offline-vendor-and-dual-mode-html/06-RESEARCH.md

@evals/run.sh
@security/policy-runtime.mjs
@security/security-policy.json
@scripts/verify-policy
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add package.json, lockfile, vendor sync script, and bootstrap vendor tree</name>
  <files>package.json, package-lock.json, vendor/walkthrough-viewer/manifest.json, vendor/walkthrough-viewer/README.md, scripts/sync-walkthrough-vendor.mjs</files>
  <read_first>.planning/phases/06-offline-vendor-and-dual-mode-html/06-RESEARCH.md, evals/run.sh, skills/walkthrough/references/html-patterns.md</read_first>
  <action>
Create **root `package.json`** with dev/deps needed to reproduce vendor assets per RESEARCH Standard Stack (pin **react/react-dom 18.x** for parity with html-patterns unless skill already upgraded — per RESEARCH Assumption A1; **mermaid@11**, **shiki** major per RESEARCH; **tailwindcss** if CLI builds precompiled CSS for **D-01**).

Run `npm install` once to generate **`package-lock.json`** — commit both. Document in `vendor/walkthrough-viewer/README.md`:
- Purpose of `vendor/walkthrough-viewer/`
- Command sequence: `npm ci` then `node scripts/sync-walkthrough-vendor.mjs` (or chosen name)
- What directories are populated (`umd/`, `css/`, `shiki-pkg/`, etc. per RESEARCH tree)

Implement **`scripts/sync-walkthrough-vendor.mjs`** (name adjustable but must live under `scripts/`):
- Copies/nprepares npm packages into `vendor/walkthrough-viewer/` layout agreed in RESEARCH (UMD bundles, precompiled Tailwind CSS artifact, Shiki ESM tree including WASM assets — **D-02**).
- Regenerates **`vendor/walkthrough-viewer/manifest.json`** with SHA-256 for every tracked byte file (algorithm entries matching `verifyVendorManifest` from 06-01).

Ensure `.gitignore` does **not** ignore `vendor/walkthrough-viewer/` tracked content; **do** ignore `node_modules/` if not already.

Granular policy keys are already in `security-policy.json`; ensure default policy in repo matches what CI/eval expects during rollout (coordinate with 06-01 defaults — if vendor not yet populated, defaults may stay `cdn` until sync completes; executor must leave repo in consistent state **after** this task with real hashes in manifest).

**Concrete paths:** Use `vendor/walkthrough-viewer/` as single root per RESEARCH — subdirs `umd/`, `css/walkthrough-viewer.css` (or chosen name), `shiki-pkg/` as needed.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f package-lock.json && test -f vendor/walkthrough-viewer/manifest.json && node --input-type=module -e "import { verifyVendorManifest } from './security/policy-runtime.mjs'; import { fileURLToPath } from 'node:url'; const r = verifyVendorManifest(fileURLToPath(new URL('.', import.meta.url))); if(!r.ok){console.error(r); process.exit(1)} console.log('manifest ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `package-lock.json` exists at repo root
    - `vendor/walkthrough-viewer/manifest.json` exists with entries covering shipped files
    - `verifyVendorManifest` passes against repo root
  </acceptance_criteria>
  <done>OFF-02 frozen layout + manifest; OFF-03 reproducible via lockfile + documented sync script.</done>
</task>

<task type="auto">
  <name>Task 2: Extend evals/run.sh with vendor preflight, copy, and strict fail-closed behavior</name>
  <files>evals/run.sh</files>
  <read_first>evals/run.sh, security/policy-runtime.mjs, scripts/verify-policy</read_first>
  <action>
Edit **`evals/run.sh`** after existing **`bash scripts/verify-policy`** success path (lines ~44–48 per RESEARCH):

1. **Resolve policy**: invoke Node (`--input-type=module`) importing `policy-runtime.mjs` to determine whether **any** package requires **vendor** `assetSource` per 06-01 exports.

2. **If vendor required**: call **`verifyVendorManifest`** against `"$PROJECT_DIR"` — on failure print actionable errors (align wording with existing security stderr style) and **`exit 1`** (**D-04** — no fallback to CDN).

3. **Copy vendor tree**: before `claude -p`, mirror pattern used for `skills/` copy — `mkdir -p "$WORK_DIR/vendor"` and `cp -R "$PROJECT_DIR/vendor/walkthrough-viewer" "$WORK_DIR/vendor/"` (adjust exact destination so generated HTML paths `./vendor/walkthrough-viewer/...` resolve after cwd/layout used by eval).

4. **If vendor not required**: skip copy (CDN-only eval remains lightweight).

5. Preserve **`assertEvalWorkspaceDirAllowed`** and existing temp-dir creation order — vendor verification must run **after** policy verified and **before** spawning expensive work.

Shell hygiene: `set -euo pipefail` already present; ensure new subshells propagate failures.

Optional: add **`bash -n evals/run.sh`** to verification for syntax.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bash -n evals/run.sh && bash scripts/verify-policy</automated>
  </verify>
  <acceptance_criteria>
    - `evals/run.sh` contains vendor copy guarded by policy resolution
    - Script exits non-zero when test forces bad manifest (manual negative check documented in SUMMARY if automated negative test not committed — prefer small `scripts/test-runsh-vendor-fail.sh` only if low overhead)
  </acceptance_criteria>
  <done>OFF-06: vendored assets included in isolated runs with fail-closed preflight per D-04.</done>
</task>

<task type="auto">
  <name>Task 3: Negative-path verification for fail-closed harness (automated where feasible)</name>
  <files>scripts/test-vendor-failclosed.mjs</files>
  <read_first>evals/run.sh, security/policy-runtime.mjs</read_first>
  <action>
Add a **small Node script** under `scripts/` (e.g. `test-vendor-failclosed.mjs`) that:
- Temporarily simulates or invokes the **same preflight condition** as run.sh: corrupt one manifest entry or missing file under a **temp copy** of vendor tree and confirm `verifyVendorManifest` returns `ok: false`.
- Exit 0 when tamper is detected; exit 1 if tamper incorrectly passes.

Alternatively, if run.sh exposes only bash, use **env override** pattern documented in SUMMARY to point `PROJECT_DIR` at a fixture directory — choose the approach that fits existing harness with minimal fake `claude`.

Keep scope minimal: prove fail-closed **manifest verification** path used by run.sh cannot succeed with wrong hashes.

Update **`files_modified`** in frontmatter if additional files created.

If full `evals/run.sh` end-to-end negative test is impossible without `claude`, restrict automated proof to `verifyVendorManifest` + documented manual grep for `exit 1` paths in run.sh — executor must document in SUMMARY.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node scripts/test-vendor-failclosed.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/test-vendor-failclosed.mjs` exits 0
    - Script proves tampered manifest fails verification
  </acceptance_criteria>
  <done>D-04 verification is observable in automation; maintainer trust in fail-closed path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendor bytes → eval workspace | Tampered or partial vendor tree must not reach eval without detection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | T | vendor files | mitigate | manifest SHA-256 verify before copy |
| T-06-08 | E | run.sh bypass | mitigate | Conditional copy only after verify; exit 1 on policy/manifest failure (**D-04**) |
| T-06-09 | R | npm supply chain | accept | Lockfile + documented versions; signing deferred to Phase 8 |
</threat_model>

<verification>
- [ ] `bash scripts/verify-policy`
- [ ] `bash -n evals/run.sh`
- [ ] `node scripts/test-vendor-failclosed.mjs`
- [ ] `npm ci` succeeds from clean `node_modules` delete (document in SUMMARY if runner cannot delete node_modules)
</verification>

<success_criteria>
- Reproducible vendor bootstrap (OFF-03) and enforced harness behavior (OFF-06) with strict failure modes (D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/06-offline-vendor-and-dual-mode-html/06-03-SUMMARY.md`
</output>
