---
phase: 02-runtime-enforcement-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - security/security-policy.json
  - security/security-policy.schema.json
  - security/policy-runtime.mjs
  - evals/run.sh
autonomous: true
requirements:
  - RUN-02
user_setup: []
must_haves:
  truths:
    - "Policy documents allowed subprocess/channel constraints or workspace path rules consumed by policy-runtime."
    - "evals/run.sh respects documented boundaries when staging temp workspaces (fail-closed on violation)."
  artifacts:
    - path: "security/security-policy.json"
      provides: "runtime execution constraint fields"
    - path: "security/policy-runtime.mjs"
      provides: "assertions for RUN-02"
  key_links:
    - from: "evals/run.sh"
      to: "security/policy-runtime.mjs"
      via: "source or node helper"
      pattern: "bounded paths"
---

<objective>
Extend the machine-readable policy and shared runtime so execution contexts are explicit: what may be invoked and where filesystem operations for eval staging are allowed (RUN-02).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-runtime-enforcement-hardening/02-CONTEXT.md
@.planning/phases/02-runtime-enforcement-hardening/02-RESEARCH.md
@security/policy-runtime.mjs
@security/security-policy.schema.json

Add minimal schema fields for runtime execution (e.g. allowed eval subprocess names or channel ids already partially modeled under egress). Implement `assertRuntimePaths` or similar: validate temp/copy targets resolve under repo or allowed prefixes. Wire `evals/run.sh` to call a small Node one-liner or source-checked path validation before bulk copy. Keep backward compatibility: existing policy files validate with defaults that match current behavior.
</context>

<tasks>

<task type="auto">
  <name>Schema and policy JSON</name>
  <files>security/security-policy.schema.json, security/security-policy.json</files>
  <action>
Extend schema with optional `runtime` object: e.g. `allowedWorkspaceRoots` or reuse/pair with `entrypoints`. Document defaults. Update `security-policy.json` with explicit values matching repo layout.
  </action>
  <verify>
    <automated>node security/verify-policy.mjs</automated>
  </verify>
  <done>verify-policy passes.</done>
</task>

<task type="auto">
  <name>policy-runtime assertions</name>
  <files>security/policy-runtime.mjs</files>
  <action>
Export helpers that resolve absolute paths and assert they fall under allowed roots from policy. Call from Node entrypoints where temp HTML/output paths are accepted.
  </action>
  <verify>
    <automated>node --check security/policy-runtime.mjs</automated>
  </verify>
  <done>Functions exported and used at least from one grader or documented hook point.</done>
</task>

<task type="auto">
  <name>run.sh staging boundary</name>
  <files>evals/run.sh</files>
  <action>
After computing temp workspace path, validate prefix against repo root (compare real paths) before `cp -R`; abort with message if path escapes.
  </action>
  <verify>
    <automated>bash -n evals/run.sh</automated>
  </verify>
  <done>Guard present before copy loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Temp workspace paths | Must stay under repo or policy-listed roots |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate |
|-----------|----------|-----------|-------------|----------|
| T2-03 | Elevation | run.sh temp dir | MITIGATED | Path canonicalization + allowlist |

</threat_model>
