---
phase: 02-runtime-enforcement-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - security/policy-runtime.mjs
  - evals/run.sh
  - evals/graders/deterministic.mjs
  - evals/report.mjs
autonomous: true
requirements:
  - RUN-03
user_setup: []
must_haves:
  truths:
    - "Gated Node scripts fail fast on insecure cwd or missing prereqs before main work."
    - "evals/run.sh exits early with clear errors when prerequisites are missing."
  artifacts:
    - path: "security/policy-runtime.mjs"
      provides: "assertSafeRepoCwd or equivalent"
  key_links:
    - from: "evals/run.sh"
      to: "scripts/verify-policy"
      via: "preflight order"
      pattern: "prereq checks"
---

<objective>
Add startup self-checks so insecure configuration is detected before prompts or grading run (RUN-03), aligned with Phase 1 verify-policy preflight.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-runtime-enforcement-hardening/02-CONTEXT.md
@evals/run.sh
@evals/graders/deterministic.mjs
@evals/report.mjs

Export optional `assertRepoInvocationContext(importMetaUrl)` that compares cwd to repo root (allow mismatch only when documented). Invoke from deterministic.mjs and report.mjs after entrypoint assert. In run.sh, after verify-policy, check commands exist (`command -v node`, `command -v claude` when not skipping LLM path). Messages must cite remediation (README or verify-policy).
</context>

<tasks>

<task type="auto">
  <name>Node startup context</name>
  <files>security/policy-runtime.mjs, evals/graders/deterministic.mjs, evals/report.mjs</files>
  <action>
Add cwd/repo-root consistency check (warn vs throw per discretion — default fail-closed for CI-like misuse). Ensure llm-rubric already loads policy early; add same helper call if not redundant.
  </action>
  <verify>
    <automated>node --check evals/graders/deterministic.mjs &amp;&amp; node --check evals/report.mjs</automated>
  </verify>
  <done>Both graders call new guard where appropriate.</done>
</task>

<task type="auto">
  <name>run.sh prerequisite gate</name>
  <files>evals/run.sh</files>
  <action>
Early exit if `node` missing. If pipeline requires `claude`, gate on its presence unless SKIP_LLM or subset mode skips generation path — match existing flags documented in script header.
  </action>
  <verify>
    <automated>bash -n evals/run.sh</automated>
  </verify>
  <done>Document new checks in script comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer cwd | Wrong directory causes misleading paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate |
|-----------|----------|-----------|-------------|----------|
| T2-04 | Information disclosure | stderr messages | LOW | Avoid leaking unrelated paths |

</threat_model>
