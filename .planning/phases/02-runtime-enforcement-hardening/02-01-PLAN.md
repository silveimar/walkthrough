---
phase: 02-runtime-enforcement-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - evals/graders/llm-rubric.mjs
  - evals/run.sh
autonomous: true
requirements:
  - RUN-01
user_setup: []
must_haves:
  truths:
    - "llm-rubric.mjs invokes claude without a shell-interpreted pipeline (no execSync of sh -c with cat|)."
    - "evals/run.sh avoids non-portable tail usage and keeps dynamic path expansion quoted-safe."
  artifacts:
    - path: "evals/graders/llm-rubric.mjs"
      provides: "spawnSync/execFile-style Claude invocation with argv array and stdin/schema via files"
      contains: "spawnSync|execFile"
    - path: "evals/run.sh"
      provides: "POSIX-friendly commands where flagged"
      contains: "tail -n"
  key_links:
    - from: "evals/graders/llm-rubric.mjs"
      to: "claude CLI"
      via: "subprocess argv"
      pattern: "no shell pipeline"
---

<objective>
Eliminate shell-mediated command composition for the LLM rubric grader (RUN-01) and fix known Bash portability issues in `evals/run.sh` so dynamic paths are never interpreted by `/bin/sh`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-runtime-enforcement-hardening/02-CONTEXT.md
@evals/graders/llm-rubric.mjs
@evals/run.sh

Replace `execSync(\`cat ... | claude ...\`)` with `spawnSync` from `node:child_process` using `shell: false`, argv array for `claude`, prompt fed via `stdin` or `--append-system-prompt` patterns supported by CLI; schema path passed as a separate argv element. Preserve existing policy guards (`assertBeforeLlmRubricExec`). grep `evals/run.sh` for `tail -` and standardize to POSIX `tail -n`.
</context>

<tasks>

<task type="auto">
  <name>Refactor llm-rubric Claude invocation</name>
  <files>evals/graders/llm-rubric.mjs</files>
  <action>
Remove shell pipeline. Use spawnSync with executable `claude`, arguments built as array (include `-p`, model, `--output-format`, `json`, `--json-schema` with file path to tmp schema, `--no-session-persistence`, budget flag). Write prompt to tmp file and pass via stdin using spawnSync input option OR use CLI-supported file input if available. Ensure tmp paths remain under output dir parent. Handle stderr/exit codes; preserve JSON parse behavior for wrapped results.
  </action>
  <verify>
    <automated>node --check evals/graders/llm-rubric.mjs</automated>
  </verify>
  <done>No execSync with shell string containing pipe; tests/manual smoke optional.</done>
</task>

<task type="auto">
  <name>run.sh portability</name>
  <files>evals/run.sh</files>
  <action>
Find and replace non-portable `tail` invocations with `tail -n N`. Review other GNU-only flags if any. Do not change eval behavior beyond portability.
  </action>
  <verify>
    <automated>bash -n evals/run.sh</automated>
  </verify>
  <done>bash -n passes; tail uses -n form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Temp prompt/schema files → Claude | Paths must be controlled; no user-controlled path in argv |
| Policy guards → exec | Existing LLM opt-in remains authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate |
|-----------|----------|-----------|-------------|----------|
| T2-01 | Tampering | llm-rubric | MITIGATED | No shell; argv-only invocation |
| T2-02 | Elevation | run.sh | LOW | Quoting + POSIX tail |

</threat_model>
